version: 2.1

# Define the jobs we want to run for this project
jobs:
  build:
    docker:
      - image: nodejs:latest
    steps:
      - checkout
      - run: docker build ./
workflows:
  build_and_test:
    jobs:
      - build

